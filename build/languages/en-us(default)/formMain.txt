:FormMain

EndTurn {
End
turn}
Return = Return

NotBuilding = No construction project
SiteDetails = Details

AsteriodName {
Asteroid belt at {=bodyName}}

GasGiantName {
Moons of {=bodyName}}

RockName {
{=bodyName}}

BuildingsPerTurn {
{countDec}{countPrefix} per turn}
countDec    ? count < 10; #D1 floor(count * 10) / 10;
countPrefix ? count ≥ 10; #T_ count;

Eta {
In {etaDec}{etaPrefix} turn{pluralDec}{pluralPrefix}}
etaDec    ? eta < 10; #D1 ceil(eta * 10) / 10;
etaPrefix ? eta ≥ 10; #T_ eta;
pluralDec    ? (eta < 10) ∧ (ceil(eta * 10) % 10 ≠ 1); =s
pluralPrefix ? (eta ≥ 10) ∧ ((eta < 20) ∨ (round(eta) % 10 ≠ 1)); =s

EtaNever = Never

MainMenu = Main menu
DevelopmentMenu = Development
-
{ ShipFormulas
	evasion # thrust;
	hitPoints # hullHp * armorFactor * (1 + 0.5 * heavyArmor);
	
	combatSpeed # thrust;
	
	detection # sensor;
	
	colonyPop # 500 * colony * (Colonizer + 1) ^ 2;
	colonyBuildings [
		Infra # 500 * colony * (Colonizer + 1) ^ 2;
	]
}